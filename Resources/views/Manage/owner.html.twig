{# src/IServ/ExerciseBundle/Resources/views/Exercise/change.html.twig #}
{% extends "IServExerciseBundle::base.html.twig" %}

{% block page_content %}

    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ _('Change owner') }}</h3>
        </div>
        <div class="panel-body">
            <div class="alert alert-warning">
                {{ _('Once you\'ve changed the owner of your exercise you can no longer manage it!') }}
            </div>

            {{ form(form) }}
        </div>
    </div>


{% endblock page_content %}
